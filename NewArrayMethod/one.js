let enames=["vg","rg","pg"];
let nums=[10,38,67,45];
enames.forEach((enames)=>{
    console.log("GM");
    console.log(enames.toUpperCase());
})
// Iterate array and execute provided function for every array element 
enames.map((enames)=>{
    console.log(enames.toUpperCase());
})